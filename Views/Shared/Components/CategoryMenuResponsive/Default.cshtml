@model IEnumerable<Article.DomainModel.Query.Category.CategoryMenuItemModel>
<nav class="navbar direction-ltr fixed-top header-responsive">
    <div class="container">
        <div class="navbar-translate">
            <a class="navbar-brand" href="#pablo">
                @*<img src="~/ClientAssets/assets/img/logo.png" height="24px" alt="">*@
            </a>
            <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse"
                    data-target="#navigation" aria-controls="navigation-index" aria-expanded="false"
                    aria-label="Toggle navigation">
                <span class="navbar-toggler-bar bar1"></span>
                <span class="navbar-toggler-bar bar2"></span>
                <span class="navbar-toggler-bar bar3"></span>
            </button>
            <div class="search-nav default">
                <!--<form action="">
                    <input type="text" placeholder="جستجو ...">
                    <button type="submit"><img src="~/ClientAssets/assets/img/search.png" alt=""></button>
                </form>-->
                <ul>
                    <li><a href='@Url.Action("Login","Account")'><i class="now-ui-icons users_single-02"></i></a></li>
                    <!--<li><a href="#"><i class="now-ui-icons shopping_basket"></i></a></li>-->
                </ul>
            </div>
        </div>
        <div class="collapse navbar-collapse justify-content-end" id="navigation">
            <!--
                <div class="logo-nav-res default text-center">
                  <a href="#">
                      <img src="~/ClientAssets/assets/img/logo.png" height="36px" alt="">
                  </a>
            </div>-->
            <ul class="navbar-nav default">
                @foreach (var Layer1 in Model)
                {
                    <li class="sub-menu">
                        <a>@Layer1.CategoryName</a>
                        <ul>
                            @foreach (var Layer2 in Layer1.Children)
                            {
                                <li class="sub-menu">
                                    <a href='@Url.Action("ClientMenuSearch","Home",new{CategoryID = @Layer2.CategoryID })'><span>@Layer2.CategoryName</span></a>
                                </li>
                            }
                        </ul>
                    </li>
                }
            </ul>
        </div>
    </div>
</nav>